# name: Build and Push Docker Image to ECR

# on:
#   push:
#     branches:
#       - "*"           # build for all branches (good for testing PRs)
#   workflow_dispatch:

# jobs:
#   build-and-push:
#     runs-on: ubuntu-latest

#     permissions:
#       id-token: write
#       contents: read

#     env:
#       AWS_REGION: us-east-1
#       ECR_REPOSITORY: demo/lfb
#       AWS_ACCOUNT_ID: 202279973546

#     outputs:
#       image_uri: ${{ steps.build.outputs.image_uri }}
#       image_tag: ${{ steps.meta.outputs.tag }}

#     steps:
#       - name: Checkout code
#         uses: actions/checkout@v4

#       - name: Configure AWS credentials (OIDC)
#         uses: aws-actions/configure-aws-credentials@v4
#         with:
#           role-to-assume: arn:aws:iam::${{ env.AWS_ACCOUNT_ID }}:role/GitHubActions-ECR-Push
#           aws-region:     ${{ env.AWS_REGION }}

#       - name: Login to Amazon ECR
#         uses: aws-actions/amazon-ecr-login@v2

#       - name: Generate short commit SHA tag
#         id: meta
#         run: echo "tag=${GITHUB_SHA::8}" >> "$GITHUB_OUTPUT"

#       - name: Build & push Docker image
#         id: build
#         run: |
#           IMAGE_TAG="${{ steps.meta.outputs.tag }}"
#           IMAGE_URI="${{ env.AWS_ACCOUNT_ID }}.dkr.ecr.${{ env.AWS_REGION }}.amazonaws.com/${{ env.ECR_REPOSITORY }}:${IMAGE_TAG}"

#           echo "Building → $IMAGE_URI"

#           docker build -t "$IMAGE_URI" .
#           docker push "$IMAGE_URI"

#           # Pass full image URI to job outputs (very useful for deploy workflows)
#           echo "image_uri=$IMAGE_URI" >> "$GITHUB_OUTPUT"

#       # Optional but recommended: show summary in workflow run
#       - name: Show built image
#         run: |
#           echo "Successfully built and pushed:"
#           echo "  ${{ steps.build.outputs.image_uri }}"