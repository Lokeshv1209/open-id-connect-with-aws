apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: lfb-ingress
  namespace: default
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  ingressClassName: nginx
  rules:
  - host: lfb.local
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: lfb-service
            port:
              number: 80
